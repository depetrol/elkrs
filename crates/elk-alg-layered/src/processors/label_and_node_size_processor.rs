//! TODO: not ported yet.

use crate::graph::{LGraphArena, LGraphId};

#[allow(unused)]
pub fn process(a: &mut LGraphArena, graph: LGraphId) -> Result<(), String> {
    Err("TODO: label_and_node_size_processor is not ported yet".to_string())
}
