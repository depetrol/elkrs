//! TODO: not ported yet.

use elk_core::javacompat::JavaRandom;

use crate::graph::{LGraphArena, LGraphId};

#[allow(unused)]
pub fn process(a: &mut LGraphArena, graph: LGraphId, random: &mut JavaRandom) -> Result<(), String> {
    Err("TODO: p3order/layer_sweep is not ported yet".to_string())
}
