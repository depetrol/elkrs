//! TODO: not ported yet.

use elk_core::javacompat::JavaRandom;

use crate::graph::{LGraphArena, LGraphId};

#[allow(unused)]
pub fn process(a: &mut LGraphArena, graph: LGraphId) -> Result<(), String> {
    Err("TODO: p5edges/orthogonal is not ported yet".to_string())
}
